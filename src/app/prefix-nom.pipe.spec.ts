import { PrefixNomPipe } from './prefix-nom.pipe';

describe('PrefixNomPipe', () => {
  it('create an instance', () => {
    const pipe = new PrefixNomPipe();
    expect(pipe).toBeTruthy();
  });
});
